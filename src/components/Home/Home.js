'use strict';

var React = require('react');

const Home = React.createClass({
    render: function() {
        return (
            <h1> Welcome to react.js </h1>
        );
    }
});

export default Home;
